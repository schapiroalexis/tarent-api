const { Course } = require("./Course");
const { Schedule } = require("./Schedule");
const { Enrollment } = require("./Enrollment");
const { Student } = require("./Student");

module.exports = { Course, Schedule, Enrollment, Student };
